export class LiquidAtmosphere{constructor(canvas){this.c=canvas;this.x=canvas.getContext('2d');this.t=0;this.resize();addEventListener('resize',()=>this.resize());this.draw()}resize(){const d=Math.min(devicePixelRatio,1.5);this.c.width=innerWidth*d;this.c.height=innerHeight*d;this.x.setTransform(d,0,0,d,0,0)}draw(){this.t+=.012;this.x.clearRect(0,0,innerWidth,innerHeight);const y=innerHeight*.83;for(let i=0;i<4;i++){this.x.beginPath();for(let x=0;x<=innerWidth;x+=18){const yy=y+i*7+Math.sin(x*.012+this.t+i)*2;if(x===0)this.x.moveTo(x,yy);else this.x.lineTo(x,yy)}this.x.strokeStyle=`rgba(145,216,207,${.035-i*.006})`;this.x.stroke()}requestAnimationFrame(()=>this.draw())}}
